# A-OS Frameworks

This folder contains framework-specific documentation for the Africa Offline OS (A-OS).

The canonical definitions of Frameworks A–E live in `docs/01_roles.md` (Binding Contract). The documents here translate that contract into implementation checklists, enforcement mechanisms, and A-OS-specific execution plans.
